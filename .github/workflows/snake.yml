name: Generate Snake

on:
  schedule:
    - cron: "0 */12 * * *"
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Generate Snake (light & dark)
        uses: Platane/snk@v3
        with:
          github_user_name: nardi-js

          # Output SVG files
          outputs: |
            dist/snake.svg
            dist/snake-dark.svg

          # Dark mode palette (IMPORTANT)
          colors: dark

      - name: Deploy to Output Branch
        uses: crazy-max/ghaction-github-pages@v2
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
